// boolean
// true or false

const amIFat = true; // !== "true"
console.log(amIFat);

// 언제 사용할까?
// 예) 사용자가 로그인 했는가?
// 예) 웹사이트가 로딩되고 있는가?

// unll
// 아무것도 없음 / 변수에 아무것도 없음
// null !== false >>> false라는 값이 있는 것
// null은 자연적으로 발생하지 않는다
// 변수 안에 어떤 것이 없다는 것을 확실하기 위해 쓴다
// 자바스크립트에 값이 없다라는 것을 알려주기 위해서

// undefined
// 변수의 값을 할당하지 않은 경우
// 변수가 존재하지만 값이 존재하지 않는 것
let something

// 두번째로 중요한 타입을 배웠다
// true, false, null, undefined
// true === on
// false === off
// null === nothing
// undefinde === yes variable no value